---
all:
  children:
    guacamole:
      hosts:
        guacamole-vm:
          ansible_host: "{{ guacamole_ip }}"
          ansible_user: admin
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_guacamole
    proxmox:
      hosts:
        pve.lab.local:
          ansible_host: 192.168.100.200
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_proxmox
